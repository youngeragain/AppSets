package xcj.app.userinfo.dao.mysql.readme

object md {
    /**
     *
     * 使用MyBatis时:
     * 如果DAO 接口添加了注解@Mapper,则可以不使用@MapperScan扫描dao接口
     * 可以不添加@Repository或者@Component注解在dao上面，因为 MyBaits会将代理对象生成后加入spring容器
     *
     */
    const val test = 0
}